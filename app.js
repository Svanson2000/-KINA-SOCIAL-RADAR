const buttons=[...document.querySelectorAll('nav button')],views=[...document.querySelectorAll('.view')];function go(id){buttons.forEach(b=>b.classList.toggle('active',b.dataset.view===id));views.forEach(v=>v.classList.toggle('active',v.id===id));}buttons.forEach(b=>b.onclick=()=>go(b.dataset.view));document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>go(b.dataset.go));const file=document.getElementById('file');file.onchange=()=>{if(file.files.length){document.getElementById('boostResult').classList.remove('hidden');document.getElementById('drop').querySelector('h3').textContent=file.files[0].name;}};